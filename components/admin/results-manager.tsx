"use client";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import type { AdminDictionary } from "@/lib/admin-i18n";
import { csrfFromDocument } from "@/lib/csrf-client";

type Year={id:string;year:number;isPublished:boolean;isDefault:boolean;_count:{candidates:number;imports:number}};
export function ResultsManager({years,dict}:{years:Year[];dict:AdminDictionary}){const router=useRouter();async function mutate(id:string,action:string,method="PATCH"){if(action==="delete"&&!confirm(dict.confirmDelete))return;const response=await fetch(`/api/admin/years/${id}`,{method,headers:{"content-type":"application/json","x-csrf-token":csrfFromDocument()},body:method==="PATCH"?JSON.stringify({action}):undefined});const data=await response.json();if(!response.ok)toast.error(data.error==="EMPTY_YEAR"?dict.emptyYear:dict.serviceUnavailable);else{toast.success(dict.saved);router.refresh()}}return <div><span className="eyebrow">{dict.manage}</span><h1 className="mt-2 text-3xl font-black">{dict.years}</h1><div className="mt-7 space-y-4">{years.map(y=><article className="surface flex flex-col justify-between gap-5 p-5 lg:flex-row lg:items-center" key={y.id}><div><div className="flex items-center gap-2"><h2 className="text-2xl font-black">BAC {y.year}</h2><span className={`badge ${!y.isPublished?"fail":""}`}>{y.isPublished?dict.published:dict.draft}</span>{y.isDefault&&<span className="badge">{dict.default}</span>}</div><p className="muted mt-2 text-sm">{y._count.candidates} {dict.candidates} · {y._count.imports} {dict.imports}</p></div><div className="flex flex-wrap gap-2"><button className="button secondary" onClick={()=>mutate(y.id,y.isPublished?"hide":"publish")}>{y.isPublished?dict.hide:dict.publish}</button>{!y.isDefault&&<button className="button secondary" onClick={()=>mutate(y.id,"default")}>{dict.makeDefault}</button>}<button className="button secondary !text-[var(--danger)]" onClick={()=>mutate(y.id,"delete","DELETE")}>{dict.delete}</button></div></article>)}</div></div>}
